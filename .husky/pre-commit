pnpm lint --fix
# Note: Temporarily disabled typecheck due to upstream issue with @ethereumjs/evm@10.0.0
# The published package contains .ts source files with .ts import extensions, causing TS5097 errors
# Re-enable once @ethereumjs fixes their package publishing
